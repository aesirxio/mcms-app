class SummaryUtils {
  transformPersonaResponseIntoModel = (response) => {
    return response;
  };

  transformResponseIntoSearchItems = (response) => {
    return response;
  };
}

const utils = new SummaryUtils();

export default utils;
