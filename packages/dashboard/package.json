{
  "name": "aesirx-cms-app",
  "version": "1.0.0-alpha",
  "license": "GPL-3.0-only",
  "author": "AesirX",
  "repository": "https://github.com/aesirxio/mcms-app",
  "dependencies": {},
  "scripts": {
    "start": "yarn run build && serve -s build",
    "build": "yarn run build:ckeditor && yarn run build:lib && react-scripts build",
    "build:lib": "cd packages/dashboard/node_modules/aesirx-dma-lib && yarn install && yarn run build && cd ../../../../",
    "build:ckeditor": "cd packages/dashboard/src/ckeditor && npm run build && cd ../../../../",
    "test": "react-scripts test",
    "dev": "react-scripts start"
  },
  "devDependencies": {}
}
